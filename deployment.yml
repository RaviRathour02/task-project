apiVersion: apps/v1 
kind: Deployment
metadata:
  name: java-app-deploy
  labels: 
     app: java-app

spec:
  replicas: 2 
  selector:
    matchLabels:
      app: java-app

  template:
    metadata:
      labels:
        app: java-app
    spec:
      imagePullSecrets:
      - name: nexus-regcred
      containers:
      - name: myapp
        image: 43.205.198.229:8082/java-app:latest
        ports:
        - containerPort: 8080
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
